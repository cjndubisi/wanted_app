{
  "scripts": {
    "test": "jest --collect-coverage",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "postinstall": "yarn workspace @wanted/common build"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "typescript": "~3.8"
  }
}

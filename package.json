{
  "scripts": {
    "test": "jest --collect-coverage",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "build": "yarn workspace @wanted/common build && yarn workspace @wanted/web build"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "version": "0.0.0"
}

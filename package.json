{
  "scripts": {
    "test": "jest --collect-coverage",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "start": "yarn workspace @wanted/web start"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "version": "0.0.0"
}
